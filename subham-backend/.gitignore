# Node artifact files
node_modules/
dist/

# Environment secrets — never commit
.env
.env.*
!.env.example

# One-off local scripts (never ship)
scratch/
scratch_*.js

# Temporary uploads — ignore temp files only
uploads/tmp/*
!uploads/tmp/.gitkeep

# System & IDE files
.DS_Store
Thumbs.db
*.log
