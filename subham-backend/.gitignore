# Node artifact files
node_modules/
dist/

# Environment secrets — never commit
.env
.env.*
!.env.example

# Temporary uploads — ignore temp files only
uploads/tmp/*
!uploads/tmp/.gitkeep

# System & IDE files
.DS_Store
Thumbs.db
*.log
